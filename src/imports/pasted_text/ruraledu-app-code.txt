Act as an Expert UX/UI Designer and Senior Frontend Engineer. I need an extremely detailed, production-ready frontend code/design for a 5-page React application named "RuralEdu" – an Offline-First Digital Learning Platform for rural schools.

CRITICAL DIRECTIVES (STRICTLY ENFORCED):
- ZERO WATERMARKS: No "Generated by AI", no AI signatures, no placeholder comments like "Insert code here". Provide complete, working structural code.
- DYNAMIC OFFLINE AUTO-DETECTION: The app MUST automatically listen to the browser's 'online' and 'offline' events. The UI must instantly react when the internet disconnects (showing offline banners) and when it reconnects (showing a brief "Syncing data..." toast).
- HUMANIZE & SIMPLIFY: Use standard React (functional components, useState, useEffect). Do NOT use Redux, Context, or complex architectural patterns. 
- DEMO-READY STATE: Pre-fill the app with robust static data so the complete flow (Login -> Dashboard -> Lesson -> Quiz) works without a backend.

GLOBAL DESIGN SYSTEM & STYLING LOGIC:
- Background: Warm Off-White (#FDFBF7) for the main body to reduce eye strain. Pure White (#FFFFFF) for content cards.
- Primary Brand Color: Emerald Green / Teal (#059669). Use for primary CTA buttons, active states, and success messages.
- Secondary/Warning Color: Slate Gray (#64748B) for offline states, Amber (#F59E0B) for "Syncing" states.
- Typography: Inter or Poppins. Large, highly legible fonts. 
- Spacing & Layout: High whitespace. Use flexbox/grid with gap-6 or gap-8. 
- Shapes (The "Friendly" Vibe): NO sharp square images. All hero/feature images MUST use an organic "blob" CSS border-radius (e.g., `border-radius: 50% 50% 40% 60% / 60% 40% 60% 40%`). Cards should have standard 16px rounded corners.
- Interactive Feedback: Every button must have a `hover:-translate-y-0.5`, `active:scale-95`, and a smooth `transition-all duration-200`.

PAGE-BY-PAGE MICRO-INSTRUCTIONS:

---
GLOBAL COMPONENT: The Smart Navbar (Appears on all pages)
- Left Side: A simple Book Icon + "RuralEdu" text (font-bold, text-xl).
- Right Side (The Auto-Detect Indicator): 
  - Create a dynamic pill-shaped badge. 
  - STATE 1 (Online): Background Emerald-50, Text Emerald-700, Icon: Wi-Fi ✅, Text: "Online & Synced".
  - STATE 2 (Offline - Auto-triggered): Background Slate-100, Text Slate-600, Icon: Cloud-Off ☁️, Text: "Working Offline".
  - Add a manual toggle button next to it for presentation purposes (to force toggle the state if the network doesn't drop during the demo).

---
PAGE 1: LANDING PAGE (Landing.jsx)
- Top Spacing: pt-24 pb-16.
- Hero Layout: 2-column grid on desktop, 1-column on mobile.
- Left Column (Content):
  - Badge above title: "Bridging the Digital Divide" (Emerald text, light emerald bg, rounded-full, px-4 py-1).
  - Main H1: "Education that never stops, even without internet." (text-5xl, font-extrabold, leading-tight).
  - Subtitle: "Interactive English and digital literacy modules designed for rural schools. Download once, learn anywhere." (text-lg, text-slate-600, mt-6).
  - Actions (mt-8 flex gap-4): Primary Button "Start Learning" (Solid Emerald, heavy shadow). Secondary Button "Download App" (Outline border, white bg, emerald text on hover).
- Right Column (Visual): 
  - A large placeholder image wrapper. Apply the dynamic CSS "blob" border-radius here. Add a subtle floating animation (`animate-bounce` but very slow).

---
PAGE 2: AUTHENTICATION (Auth.jsx)
- Layout: 100vh, flex center, off-white background.
- Auth Card: White bg, max-w-md, w-full, p-8, rounded-2xl, shadow-lg.
- Header: H2 "Welcome to RuralEdu" + subtext "Log in to continue your journey."
- Inputs: 
  - Email & Password fields.
  - Default state: border-slate-200. Focus state: ring-2 ring-emerald-500, outline-none.
- Action: Full-width Emerald button "Log In". On click, `e.preventDefault()` and automatically navigate to `/dashboard`.

---
PAGE 3: STUDENT DASHBOARD (Dashboard.jsx)
- Top Section: 
  - Greeting: "Welcome back, Learner 👋" (text-3xl, font-bold).
  - Progress Card: Full width, white bg, p-6. "Overall Progress: 45%". Include a visual progress bar (bg-slate-200, inner fill bg-emerald-500 w-[45%], rounded-full h-3).
- Content Grid: "Your Learning Path" (mt-12). Create a 3-column grid of `CourseCard` components.
- Course Card UI (Strict Rules):
  - White card, p-5, rounded-xl, hover:shadow-xl transition.
  - Image placeholder at top (h-32, bg-slate-100, rounded-lg).
  - Title: "Basic Computer Skills".
  - Visual Auto-Detect Logic inside Card:
    - If `isOnline` is true AND course is NOT downloaded: Show a "Cloud Download" icon button.
    - If `isOnline` is false AND course is NOT downloaded: Show a grayed-out "Requires Internet" badge.
    - If course IS downloaded: Show a permanent green "✅ Available Offline" badge, regardless of internet status.

---
PAGE 4: THE LESSON VIEW (Lesson.jsx)
- Layout: Max-width 4xl, centered.
- Top: Simple "← Back to Dashboard" text link (text-slate-500 hover:text-emerald-600).
- Media Player Placeholder: 
  - A large dark rectangle (bg-slate-800, aspect-video, rounded-xl, flex-center). 
  - Put a Play button icon in the center.
- Details Section (mt-6):
  - H1: "Chapter 1: Understanding the Internet".
  - Text description placeholder.
- Bottom Action Bar:
  - Flex space-between, sticky at bottom, bg-white, p-4, border-t.
  - Left: Download Toggle (If offline, disable this button unless already downloaded).
  - Right: "Take Chapter Quiz →" (Solid Emerald button). Navigates to `/quiz`.

---
PAGE 5: QUIZ INTERFACE (Quiz.jsx)
- Layout: Highly focused, minimal distractions. 
- Top Header: "Question 1 of 5" with a micro progress bar (20% filled).
- Question Box: Large font, center-aligned. "What does CPU stand for?" (mt-10, mb-8, text-2xl, font-semibold).
- Options (Vertical List of 4 items):
  - Box UI: border-2 border-slate-200, rounded-xl, p-4, cursor-pointer.
  - Hover state: border-emerald-300 bg-emerald-50.
  - Active/Selected state: border-emerald-600 bg-emerald-100.
- Submit Section: 
  - "Submit Answer" button (disabled until an option is clicked).
  - On submit: Show a success text "Great job! +10 points" and change button to "Next Question".

Generate the complete React code architecture with these exact Tailwind classes, offline-detection hooks, and UI states. Make it perfectly clean.